# OCR Service Configuration
# Copy this file to .env and fill in your actual values

# OCR.space API Key (required)
# Get your API key from https://ocr.space/ocrapi
# for example only (it might be working key, but don't use it in production)
OCR_API_KEY=K82475525688957

# OCR API URL (required)
OCR_API_URL=https://api.ocr.space/parse/image

# Log level (optional - defaults to 'info' in production, 'debug' in development)
LOG_LEVEL=info